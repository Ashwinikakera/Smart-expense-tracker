# Smart-expense-tracker
"Smart Expense Tracker" a REST API built with Python and FastAPI for logging personal expenses and viewing spending totals, It follows a layered architecture (routes → services → models), and persists data to a JSON file rather than a full database, making it simple to run anywhere with no setup overhead.
